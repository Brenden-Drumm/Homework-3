#include "Student.h"

//using namespace std;

Student::Student():Person(){}
Student::Student(string firstS, string lastS, int dayS, int monthS, int yearS, int sid):Person(firstS, lastS, dayS, monthS, yearS){}

void setID(ID)
{
    int sid = ID;
}

string getID(){
    return sid;
}
